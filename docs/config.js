// Конфигурация окружения мини-приложения. Правится прямо на хостинге — пересборка не нужна.
window.AUCTION_CONFIG = {
  // Адрес API аукциона (постоянный, с сертификатом Let's Encrypt).
  //   сейчас — тестовый сервер (RUVDS, Амстердам); при переезде в РФ меняется только эта строка.
  // Пустая строка = API на том же хосте, что и мини-приложение (локальная разработка).
  apiBase: "https://185-185-68-250.sslip.io",

  // Показывать демонстрационный тендер, если API недоступен (или если открыли ?startapp=demo).
  // В демо-режиме интерфейс помечается явной плашкой «ДЕМО», данные не с сервера.
  demoFallback: true,
};
